#include <stdio.h>

/**
 * main - displaying lower and upper case
 *
 * Return: 0 (success)
 */
int main(void)
{
	char l;
	char u;

	for (u = 65; u < 91; u++)
		putchar(u);
	for (l = 97; l < 123; l++)
		putchar(l);
		return (0);
	}
}
