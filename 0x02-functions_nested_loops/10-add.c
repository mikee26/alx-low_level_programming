#include "main.h"
/**
 * add - prints the addition of numers
 * @a: the character
 * @b: the second character
 * Return: the addition of a and b
 */
int add(int a, int b)
{
	int r;

	r = a + b;
	return (r);
}
