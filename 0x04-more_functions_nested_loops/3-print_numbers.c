#include "main.h"
#include <stdio.h>
/**
 * print_numbers - display form 0 to 9
 *
 * Return: the numbers
 */
void print_numbers(void)
{
	int i;

	for (i >= 48; i < 59; i++)
		_putchar(i);
	return (0);
}
